version: '3.8'

services:
  challenge:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - challenge-data:/data
    environment:
      - PORT=3000
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  challenge-data:
